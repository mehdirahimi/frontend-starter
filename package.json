{
  "name": "theme",
  "version": "1.0.0",
  "description": "Iconic Admin Panel Theme",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "npm-dev": "npm run development",
    "npm-watch": "npm run development --watch",
    "npm-prod": "npm run production",
    "webpack-build": "webpack --mode=production --config webpack.config.js",
    "build": "npm run clean && npm run webpack-build",
    "watch": "webpack --watch --progress --mode=development"
  },
  "keywords": [],
  "author": "Mehdi Rahimi <mehdirahimi@email.com>",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^4.5.3",
    "jquery": "^3.5.1",
    "popper.js": "^1.16.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "webpack": "^5.1.2",
    "webpack-cli": "^4.0.0"
  }
}
